# Finnish National Gallery Workshop - Requirements
#
# Install with: pip install -r requirements.txt
#
# For GPU support (recommended for CLIP):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# Core dependencies
requests>=2.28.0
numpy>=1.21.0
pillow>=9.0.0
tqdm>=4.64.0

# Jupyter
jupyter>=1.0.0
ipykernel>=6.0.0

# CLIP (install separately for best compatibility)
# pip install git+https://github.com/openai/CLIP.git

# PyTorch (install separately based on your system)
# See: https://pytorch.org/get-started/locally/
# For CPU only: pip install torch torchvision
# For CUDA 11.8: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
